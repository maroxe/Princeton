\documentclass[12pt]{article}

% packages
\usepackage{geometry}
\usepackage{amsmath}
\usepackage{amsfonts}

% custom commands
\newcommand{\Q}[1]{\subsubsection*{Problem #1}}

% parameters
\geometry{hmargin=1cm,vmargin=1cm}
\title{ORF524 - Problem Set 1}
\author{Bachir EL KHADIR }

\begin{document}

\maketitle

\Q{1}
\begin{align*}
\text{min} & \sum_{i,j} (x_i + x_j) w_{ij} \\
& \text{s.t} \, x_i \in \{-1, 1\}
\end{align*}

\Q{2}
\begin{itemize}
\item [1.] We can rewrite the constraints as:
    \begin{align*}
    &2 x_1 + 3 x_2 + x_3   + z_1      &= 4 \\
    &x_1           - 2 x_3      - z_2 &= 1 \\
    &x_1, x_2, x_3, z_1, z_2          &\geq 0
    \end{align*}
\item [2.] The set of feasible solution is not empty( $\{x_1=1, x_i=z_j=0\}$ is in it)  and is bounded from the above (by the first constraint), it does admit an optimal solution $(x_1, x_2, x_3, z_1, z_2)$
    Let's now consider $(x_1 , x_2 + \frac{x_3}{3}, 0, 0, 0)$. This is a feasible point that maximizes the objective.
\item [3.] 
\end{itemize}

\Q{4}
\begin{itemize}
\item [1.] $P = A^{-1}(\{b\}) \cap R^{n+}$, is closed because it's the intersection of the inverse image of a closed set(a singleton) by a continuous application and another closed set.
\item [2.] interesection of two convex sets
\item [3.] Let $y$ be an element of the feasible set.
$P$ bounded $\eq P$ bounded from above  $\eq \{x | Ax=b} = N(A) + y $ is bounded from above $eq N(A) \cap {x \geq 0} = \emptyset$  (because $N(A)$ is a vector space, if it contains some elements with 

\item[4.] If there exists an optimal solution then $P$ is bounded, so $N(A) \cap \{x \geq 0\} = \{0\}$ and since $0 \in \{d | c^Td < 0\}$, we have the equality ...
Conversely, if is not $| \emptyset$, let $d$ be an element of the intersection, and let $x$ be a feasible solution. Then $x + \lambda d$ is also a faisible solution for $\lambda \in \mathbb{R^+}$, and $c^T (x + \lambda d)$ goes to $-\infty$ as $\lambda \rightarrow + \infty$
\end{itemize}

\Q{4}
\begin{itemize}
\item [1.]
\begin{align*}
    &\text{minimize} & \sum_{(u, v) \in E} x_{u, v} w_{u, v} \\
    &\text{s.t}      & \sum_{v \in V} x_{u,v}  = 1 \, \forall u \in U \\
    &                & \sum_{u \in U} x_{u,v}  = 1 \, \forall v \in V \\
\end{align*}

\item [2.] The relaxed problem
\begin{align*}
    &\text{minimize} & \sum_{(u, v) \in E} x_{u, v} w_{u, v} \\
    &\text{s.t}      & \sum_{v \in V} x_{u,v}  = 1 \, \forall u \in U \\
    &                & \sum_{u \in U} x_{u,v}  = 1 \, \forall v \in V \\
    &                & 0 \leq x_{u,v} \leq 1 \, \forall u,v \in UxV 
\end{align*}
\end{itemize}

if in the sum $\sum_{v \in V} x_{u,v}  = 1$, more than one element is positive, replacing all of them with 0 except the element with the smallest weight (give it a value of 1) can only better the solution.

\Q{5}
Let $x_{i, j}$ be the number of cars we move from city $i$ to $j$, $w_{i,j}$ the cost of moving a car from $i$ to $j$, $p_i$ the number of cars currently present in city $i$, and $n_j$ the number of cars needed.

We want to minimize $\sum x_{i,j} w_{i,j} = w^T x$ under the constraints
$ \sum_j x_{i,j} = n_i - p_i \, \forall i$

\end{document}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:

